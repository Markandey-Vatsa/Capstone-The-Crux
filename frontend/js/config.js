// Deprecated: config.js no longer needed after UI revert.
// Left intentionally minimal to avoid 404 if referenced elsewhere accidentally.
if(!window.API_BASE_URL){ window.API_BASE_URL = 'http://localhost:8080/api'; }
